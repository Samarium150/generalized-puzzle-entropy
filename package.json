{
  "name": "generalized-puzzle-entropy",
  "license": "GPL-3.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.3.3",
    "turbo": "^2.2.3"
  },
  "engines": {
    "node": ">=18.17.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.14.0",
  "private": true,
  "pnpm": {
    "peerDependencyRules": {
      "allowAny": [
        "typescript"
      ]
    },
    "updateConfig": {
      "ignoreDependencies": [
        "long",
        "bytebuffer",
        "protobufjs"
      ]
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "playwright-chromium"
    ]
  }
}
